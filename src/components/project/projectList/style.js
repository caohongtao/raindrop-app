'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  container: {
    backgroundColor: '#FFF',
  },
  loading:{
    flex:1,
    justifyContent:'center',
    alignItems: 'center',
  },
  projectListView:{
    backgroundColor: '#FFF',
  }
});