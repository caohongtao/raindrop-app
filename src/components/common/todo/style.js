'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  container: {
    flex: 1,
  },
  todo:{
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});